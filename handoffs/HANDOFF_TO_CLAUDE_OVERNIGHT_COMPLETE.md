# Overnight Complete Handoff to Claude

Generated by Codex (`codex-impl-overnight`) on 2026-05-03.

## Summary Table

| Repo | PR | Branch | Status | Codex audit verdict | Notes |
| --- | --- | --- | --- | --- | --- |
| HermesProof | #18 | `feat/cp-hp-0.6-secret-leak-hardening` | Merged | Fixed + LGTM | Codex added fail-closed gitleaks/pre-commit hardening, tightened allowlists, aligned docs, and re-audited clean. |
| HermesProof | #19 | `feat/cp-hp-0.6-env-file-resolution` | Merged | Fixed + LGTM | Codex added existence-aware `HERMES3D_ENV_FILE` resolution and tests, then re-audited clean. |
| Hermes3D | #31 | `docs/overnight-codex-briefs` | Merged | Needs architect review | Codex comment is preserved on the closed PR; review noted structural mismatches in several briefs. |
| Hermes3D | #33 | `codex/v5.3.0-release-notes-draft` | Merged to develop | LGTM | Codex drafted release notes, CI passed, and the PR merged before this handoff was written. |
| Hermes3D | #34 | `feat/cp-h3d-mnemosyne-recall-recovered` | Open | Needs architect review | Two fresh audits found red CI, missing orchestrator integration despite the PR claim, stale ADR references, optional-dependency drift, and recall matching risk. |
| Hermes3D | #35 | `feat/cp-h3d-settings-tab-recovered` | Open | Needs architect review | CI is green and env-value redaction is clean, but audits found Settings contract drift and mock/reality mismatches. |
| Hermes3D | #37 | `feat/cp-h3d-partial-scaffolds` | Open | Needs architect review | PR is explicitly partial; audits found red CI, missing security scanner import, health UI compile failures, and registry formatting/type issues. |

## Open for Morning Review

- [Hermes3D PR #34](https://github.com/Ghenghis/Hermes3D/pull/34) — Mnemosyne recall layer. Do not merge as-is.
- [Hermes3D PR #35](https://github.com/Ghenghis/Hermes3D/pull/35) — Settings tab recovery. Green checks, but contract decision needed.
- [Hermes3D PR #37](https://github.com/Ghenghis/Hermes3D/pull/37) — partial scaffolds. Do not merge as-is; likely split into separate PRs.

No HermesProof PRs were open at the time of this report.

## Blocked or Needs Architect Review

### Hermes3D #34 — Mnemosyne Recall

Audit result: **needs architect review**.

- Layer A fails on Ruff import ordering in `03_implementation/src/hermes3d/core/agents/orchestrator.py`; Layer M fails downstream because Layer B was skipped.
- PR body says `orchestrator.py` instantiates `MnemosyneRecall`, but the diff only adds a `TYPE_CHECKING` import.
- `requirements.txt` makes `mnemosyne-memory>=2.0,<3` mandatory even though `pyproject.toml` makes it optional.
- Changed code still references ADR-014, while the added ADR is ADR-015.
- Dispatcher recall scoring uses substring matching, which can bias overlapping printer IDs.
- No tests cover wrapper fallback, recall scoring, hard-filter safety, or the claimed orchestrator injection.

Security audit found no secret leak, no `.env` or private-path reads, and no deploy/release behavior.

### Hermes3D #35 — Settings Tab

Audit result: **needs architect review**.

- CI is green and the explicit env-value redaction contract is satisfied.
- The PR replaces the existing Settings implementation with four subtabs, while `TAB_SPECS` still describes a broader Settings surface.
- `PrintersSubtab` mock entries do not match `printers.toml`.
- `ProvidersSubtab` claims to display `llm_policy.yaml`, but includes entries not configured in that policy.
- `PrintersSubtab` performs a live fetch to `/api/health/services` despite the PR saying real data wiring is follow-up; route scans did not find that endpoint.
- No Settings-specific Playwright or unit coverage was added.

Security audit found no secrets, no raw env reads, no private-path reads, and no release/deploy/workflow changes.

### Hermes3D #37 — Partial Scaffolds

Audit result: **needs architect review**.

- Layer A fails because `core/tool_registry/registry.py` is not `ruff format --check` clean.
- Layer D2 fails because `ServiceHealthPage.tsx` imports missing `ServiceCard` and passes invalid `Panel` props.
- `core/security/__init__.py` imports missing `prompt_injection_scanner.py`, so `import hermes3d.core.security` fails.
- The PR combines three unrelated surfaces: tool registry, security scanner shell, and service-health UI.
- Audit agrees with the PR body: split this into smaller PRs rather than merging as-is.

Security audit found no hardcoded secrets, no `.env` changes, no private-path reads, and no deploy/release/workflow changes.

## Skipped

- No direct fixes were pushed to Claude recovery branches because the latest boundary marked those branches read-only for Codex unless explicitly handed off.
- HermesProof v0.6 release notes were not drafted because the gate-pack work was not fully present as an open/merged v0.6 aggregate PR during this audit window.
- Cross-repo fix PRs were not opened; the active Hermes3D recovery PRs need architect decisions first.
- No release branches, tags, GitHub Releases, or auto-merges were created.

## Surprises and Observations

- The GitHub check surface can be green while UI contract drift remains real; PR #35 is the example.
- CodeRabbit release-note summaries may repeat PR claims that are not supported by repository routes or config.
- The main Hermes3D checkout was treated as Claude-owned and avoided for edits. Codex used isolated worktrees for local inspection and docs work.
- The local worktree `npm run lint` for PR #35 failed before `npm ci` because dependencies were absent; GitHub CI and the code audit worktree with dependencies showed the actual PR checks green.

## Recommended Morning Sequence

1. Review and decide the intended scope for PR #35. If four subtabs are the desired new scope, update the contract/docs and remove or justify the live fetch. If not, restore the broader Settings surface.
2. Close or split PR #37. The registry may be salvageable after formatting/type cleanup; security and service-health UI need complete follow-up PRs.
3. Rework PR #34 around a clear scope: either external `recall_hints` only, or full orchestrator-owned recall resolution and injection. Align dependency optionality and ADR references.
4. After recovery PRs are resolved, rerun the cross-repo audit for HermesProof/Hermes3D references and tool-count claims.
5. Continue only after the current read-only recovery branches are either merged, closed, or explicitly handed off.

## Evidence

- PR #34 audit comment: https://github.com/Ghenghis/Hermes3D/pull/34#issuecomment-4365971143
- PR #35 audit comment: https://github.com/Ghenghis/Hermes3D/pull/35#issuecomment-4365985335
- PR #37 audit comment: https://github.com/Ghenghis/Hermes3D/pull/37#issuecomment-4365983530

## Stop Point

Codex stopped after documenting all currently open PRs and creating this handoff. No Phase 5.3 release was cut, no release branch was opened, no production/VPS connection was attempted, and no secrets were read.
