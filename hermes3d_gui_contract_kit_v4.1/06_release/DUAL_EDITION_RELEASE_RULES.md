# Dual Edition Release Rules

Artifacts: Windows installer/portable app, worker service/tray package, Ubuntu Docker Compose bundle, systemd services, reverse proxy template, tunnel setup guide. Do not ship if desktop gates fail, VPS auth disabled, worker registration unverified, or desktop ↔ VPS sync lacks proof.
