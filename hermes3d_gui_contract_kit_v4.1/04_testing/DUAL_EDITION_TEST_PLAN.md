# Dual Edition Test Plan

Desktop gates: app launches, local API health, GPU detector, adapter detect-only gates, local proof bundle. VPS gates: deploy dry-run, auth enabled, worker registry, tunnel health, remote dry-run dispatch, proof mirror. Cross-edition gates: same tabs render, same job appears in both UIs, worker status propagates over WebSocket, remote dispatch creates proof event.
