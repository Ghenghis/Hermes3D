# Windows Desktop GPU Worker Edition

Recommended packaging: Tauri or Electron desktop shell, Python/FastAPI backend, Windows service/tray worker, local data under `%APPDATA%/Hermes3D`. Definition of done: detects RTX 3090 Ti when available, local health endpoint, UI opens locally, worker registers to VPS staging, detect-only adapter tests pass, no printer write commands until gates pass.
