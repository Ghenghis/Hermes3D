"""Hermes3D CLI surface — see ``hermes3d.cli.__main__`` for the command catalog.

Use ``python -m hermes3d.cli <command>`` to invoke. The ``main`` and
``build_parser`` functions are importable lazily via:

    from hermes3d.cli.__main__ import main, build_parser
"""

__all__: list[str] = []
