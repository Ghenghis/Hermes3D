"""Pattern catalogues for the prompt-injection scanner.

Files in this package are YAML, not Python — see
``hermes3d.core.security.injection_scanner._load_ruleset``. This ``__init__``
exists only to make the directory a regular package (so importers and
package-data tooling can locate the YAML alongside the code).
"""
