{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "hermes3d://adapter_registry/schemas/firmware_prusa.schema.json",
  "title": "Prusa-Firmware source descriptor",
  "description": "Source-only descriptor for Prusa-Firmware (MK3/MK4 family). No flashing, no serial, no USB to printer.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "source_url", "install_check", "version_pattern"],
  "properties": {
    "name": {"type": "string", "const": "prusa_firmware"},
    "source_url": {
      "type": "string",
      "format": "uri",
      "default": "https://github.com/prusa3d/Prusa-Firmware.git"
    },
    "default_branch": {"type": "string", "default": "MK3"},
    "install_check": {
      "type": "string",
      "default": "git ls-remote --heads https://github.com/prusa3d/Prusa-Firmware.git",
      "description": "Read-only remote probe. NEVER a flash command."
    },
    "version_pattern": {
      "type": "string",
      "default": "^[0-9a-f]{40}\\s+refs/heads/.+$"
    },
    "toolchain_required": {"type": "string", "const": "toolchain_avr_gcc"},
    "no_flash": {"type": "boolean", "const": true}
  }
}
