{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "hermes3d://adapter_registry/schemas/firmware_reprap.schema.json",
  "title": "RepRapFirmware source descriptor",
  "description": "Source-only descriptor for Duet3D RepRapFirmware. No flashing, no serial, no USB to printer.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "source_url", "install_check", "version_pattern"],
  "properties": {
    "name": {"type": "string", "const": "reprapfirmware"},
    "source_url": {
      "type": "string",
      "format": "uri",
      "default": "https://github.com/Duet3D/RepRapFirmware.git"
    },
    "default_branch": {"type": "string", "default": "v3.5-dev"},
    "install_check": {
      "type": "string",
      "default": "git ls-remote --heads https://github.com/Duet3D/RepRapFirmware.git",
      "description": "Read-only remote probe. NEVER a flash command."
    },
    "version_pattern": {
      "type": "string",
      "default": "^[0-9a-f]{40}\\s+refs/heads/.+$"
    },
    "toolchain_required": {"type": "string", "const": "toolchain_arm_none_eabi"},
    "no_flash": {"type": "boolean", "const": true}
  }
}
