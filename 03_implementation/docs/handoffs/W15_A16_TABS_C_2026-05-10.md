# W15-A16 — Primary Tabs C (Artifacts, Approvals, Plugins, Roadmap)

- **Date:** 2026-05-10
- **Wave / Agent:** Wave 15 — Agent 16
- **Branch:** `claude/w15-a16-tabs-c` (from `origin/develop` HEAD `3934eb8`)
- **Lock owner:** `claude-w15-a16-tabs-c`
- **Task ID:** `W15-A16-TABS-C-2026-05-10`
- **Disjoint scope:**
  - `03_implementation/ui/src/tabs/Artifacts.tsx`
  - `03_implementation/ui/src/tabs/Approvals.tsx`
  - `03_implementation/ui/src/tabs/Plugins.tsx`
  - `03_implementation/ui/src/tabs/Roadmap.tsx`
  - `03_implementation/ui/src/components/{artifacts,approvals,plugins,roadmap}/*.tsx`
  - `03_implementation/ui/playwright.w15-a16.config.ts`
  - `03_implementation/ui/tests/visual/w15-a16-tabs-c.visual.spec.ts`
  - `03_implementation/proof/screenshots/w15-a16/*.png`

---

## 1. Lane summary

All four tabs in scope already existed on `origin/develop` with substantial
backend wiring. This lane:

1. Extracted helper components per tab (per the brief) and refactored each
   tab to consume them. No backend-wiring changes; all `/api/*` calls are
   preserved bit-for-bit.
2. Added a vite-only Playwright screenshot config and spec that captures
   1536x1024 PNGs of each tab in scope at honest-blocked rest, capturing
   any console errors.
3. Updated `.gitignore` to whitelist `src/components/artifacts/` (the
   existing `artifacts/` ignore rule for Playwright outputs swallowed the
   new source dir; the whitelist preserves the original intent).

No new fake/mock data was introduced. Empty-state copy was preserved:

- `"No artifacts returned by the live artifacts API."` (Artifacts)
- `"No pending approvals returned by the live approvals API."` (Approvals)
- `"No plugins returned from the live plugin API."` (Plugins)
- `"No roadmap items returned by the live roadmap API."` (Roadmap)

---

## 2. Helper components added

| Path | Lines | Purpose |
| --- | --- | --- |
| `src/components/artifacts/ArtifactRow.tsx` | ~46 | One artifact row with type pill + name + size + View action |
| `src/components/artifacts/ArtifactList.tsx` | ~62 | Groups rows by `jobTitle`; honest empty state |
| `src/components/approvals/ApprovalRow.tsx` | ~149 | Card with file scope, requester, [Approve][Deny][Defer] + inline form |
| `src/components/approvals/ApprovalQueue.tsx` | ~57 | Feed-style grid; honest empty state |
| `src/components/plugins/PluginCard.tsx` | ~83 | Plugin tile (state badge + Config/Logs/Activate) |
| `src/components/plugins/PluginList.tsx` | ~36 | Grid of cards; honest empty state |
| `src/components/roadmap/RoadmapItem.tsx` | ~65 | Single row (number, description, state, Open button) |
| `src/components/roadmap/RoadmapBoard.tsx` | ~45 | List of rows; honest empty state |

Each helper file documents the two sources consulted (GitHub Projects /
WAI-ARIA APG / Linear / VS Code Marketplace conventions) at the top.

---

## 3. Self-audit

| gate | result |
| --- | --- |
| `npx tsc --noEmit` | **PASS** (no output) |
| `npm run build` | **PASS** — 2233 modules transformed, built in 1.30s |
| 4 screenshots at 1536x1024 | **PASS** — `03_implementation/proof/screenshots/w15-a16/{artifacts,approvals,plugins,roadmap}-1536x1024.png` |
| 0 console errors per tab | **PASS** — `*-console-errors.json` shows `errors: []` for all 4 tabs |
| Playwright spec | **PASS** — 4/4 passed in 2.5s via `playwright.w15-a16.config.ts` |

---

## 4. Backend wiring (unchanged; verified preserved)

| Tab | Endpoint(s) used |
| --- | --- |
| Artifacts | `GET /api/artifacts` (list), `GET /api/artifacts/list` (proof manifest), `POST /api/artifacts` (evidence attach), `GET /api/artifacts/{id}/download`, `GET /api/artifacts/proof/{filename}` |
| Approvals | `GET /api/approvals?status=pending`, `GET /api/approvals?status=*` (history), `POST /api/approvals/{id}/approve|reject|defer` (via `adapters.{approve,reject,defer}Approval`) |
| Plugins | `GET /api/plugins`, `POST /api/plugins/{id}/activate`, `GET /api/plugins/{id}/config`, `GET /api/plugins/{id}/logs`, `GET /api/source-os/modules/update-readiness`, `GET /api/source-os/modules/runtime-setup-queue` |
| Roadmap | `GET /api/roadmap`, `GET /api/roadmap/tab-completion`, `GET /api/source-os/modules/runtime-setup-queue` |

---

## 5. Sources consulted

Per the brief, 2 sources are required. Helpers cite:

1. **GitHub Projects conventions** — issue / PR row + grouping / status
   patterns: <https://docs.github.com/en/issues/planning-and-tracking-with-projects/learning-about-projects/about-projects>
2. **WAI-ARIA Authoring Practices (APG)** — Feed pattern, Listbox pattern,
   Button-with-tooltip pattern: <https://www.w3.org/WAI/ARIA/apg/>
3. Tab-specific:
   - **VS Code Extensions Marketplace** for Plugin card layout:
     <https://code.visualstudio.com/api/references/extension-guidelines#extension-presentation>
   - **Playwright Screenshot API** for the 1536x1024 capture spec:
     <https://playwright.dev/docs/screenshots>

---

## 6. Lock release

- `hermes_release_files` for all 12 files (`claude-w15-a16-tabs-c`) is
  the final action before this lane reports back.
- `hermes_record_outcome` is recorded with `outcome=lgtm` once PR opens.
