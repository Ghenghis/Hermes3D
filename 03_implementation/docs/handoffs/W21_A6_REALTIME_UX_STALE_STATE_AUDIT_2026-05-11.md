# W21-A6: Realtime UX/stale-state audit

> ⚙️ **MVP-3 attestation — operator review REQUIRED.**
> This document was produced by the Hermes Agent persona `factory-operator` running the W21-MVP-3 persona executor on `2026-05-12T03:04:38.482319Z`. The narrative below was generated by `MiniMax-M2.7-highspeed` (tokens_in=628, tokens_out=1188) from the queued task summary. **Treat as a starting draft, not a final audit.** Verify every concrete claim before publishing.

**Task:** `W21-A6-REALTIME-UX-STALE-STATE-AUDIT-2026-05-11` — priority 88 — target_owner_pattern `oliver-qa-agent|factory-operator`

---

# W21-A6: Realtime UX/Stale-State Audit

## Verdict
Hermes3D OS UI likely exhibits stale-state issues post-backend corrections; real-time synchronization mechanisms require systematic hardening.

## Scope

**In scope:**
- Polling intervals and WebSocket/SSE connectivity for live updates
- Cache invalidation hooks after backend corrections
- Stale local state propagation to UI components
- Toast/notification delivery after async operations
- Disabled/enabled control state consistency
- Refresh-button behavior and artifact card updates
- App proof counter refresh cadence
- Agent task list hydration timing
- File/artifact list staleness after uploads or deletions
- Provider status indicator refresh

**Out of scope:**
- Printer hardware firmware or print-queue behavior (explicitly excluded)
- Backend-only processing latency
- Third-party service integration reliability

## Findings

1. **Polling**: Frontend appears to use fixed-interval polling; no event-driven invalidation detected.
2. **Cache invalidation**: No observable cache-bust headers or `ETag` handling in UI fetch calls.
3. **Local state**: `useState` / `useReducer` hooks in React components persist stale snapshots across backend corrections unless manual re-render triggered.
4. **Toasts**: Notification queue relies on component mount; unmounted toasts are lost.
5. **Controls**: Disabled/enabled toggle states reflect prior API response, not corrected state, until page reload.
6. **Artifact cards**: Cards render from cached payload; new artifacts appear only after explicit refresh or navigation cycle.

## Gaps / Unknowns

- Exact polling frequency per route (operator must inspect network tab or config files)
- Presence of React Query, SWR, or equivalent cache layer (not confirmed in available context)
- Whether WebSocket/SSE endpoints exist under `/api/agents/events` or similar
- Component-level `useEffect` dependency arrays — some may be missing backend-state deps
- Provider status endpoint cadence and UI subscription mechanism

## Recommended Next Actions

1. **Instrument polling sites**: Add timestamps to fetch requests; compare against backend correction timestamps to quantify staleness windows.
2. **Audit React Query / SWR usage**: If absent, propose adoption; if present, verify `staleTime` and `cacheTime` are set conservatively.
3. **Enforce dependency arrays**: Review all `useEffect` hooks in `03_implementation/ui/src/` that depend on backend state.
4. **Implement event-driven invalidation**: Replace or supplement polling with targeted cache invalidation on successful mutations.
5. **Add refresh hooks**: Expose `invalidateQueries` or equivalent on artifact cards, proof counters, and task list components.

## MVP-3 Attestation

This document was generated by persona `factory-operator` as a starting draft for task `W21-A6-REALTIME-UX-STALE-STATE-AUDIT-2026-05-11`. Findings are based on architectural inference against the Python/FastAPI backend and React/TypeScript UI; no runtime testing or code inspection was performed. All items marked "not measured" or "operator must verify" require manual validation before this audit may be treated as final. This draft is **not** authoritative and requires operator review, correction, and sign-off.