# research/ — index pointer

This folder holds the H3D OS folder-index for `G:\Github\_research\`.

See **[`_research.md`](./_research.md)** for the full inventory, topic categorisation, references, and SVG diagram.

Indexed: 2026-05-07. Source folder: `G:\Github\_research\` (single embedded checkout: `atomic-hermes/`, a clone of `NousResearch/hermes-agent`). The parent `_research/` is **not** a git repo; the embedded `atomic-hermes/` is.
