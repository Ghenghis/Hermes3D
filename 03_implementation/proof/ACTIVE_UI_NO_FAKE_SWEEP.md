# Active UI No-Fake Sweep

Worker: E

Scope:
- Entry point: `03_implementation/ui/src/App.tsx`
- Production graph: relative imports reachable from the active 16-tab app shell.
- Excluded by design: test files, comments, inactive source files that are not imported by the active app graph, and `data/mock` module contents after a forbidden import is detected.

Verification:
- `python 03_implementation/scripts/scan_active_ui_no_fake.py`
- `python 03_implementation/scripts/scan_active_ui_no_fake.py --json`

Rules enforced:
- Any active production import from `data/mock` fails.
- Any active production string/template literal containing visible fake-mode terms fails.
- Forbidden terms: `mock`, `mocked`, `fake`, `faked`, `simulated`, `simulation`, `demo`, `sample`, `placeholder`, `dummy`, `fixture`, `lorem`.

Observed result on 2026-05-06:
- The latest scan walked 81 active production UI files.
- The scan passed with 0 findings.
- No active production import from `data/mock` remains.
- No active production string/template literal contains the blocked fake-mode terms.

Observed result on 2026-05-08:
- Command: `python 03_implementation/scripts/scan_active_ui_no_fake.py --repo-root <worktree-root>`
- The scan walked 81 active production UI files reachable from the App.tsx entry point.
- The scan passed with 0 findings.
- No active production import from `data/mock` remains.
- No active production string/template literal contains the blocked fake-mode terms.
- Coverage note: 96 total .ts/.tsx/.js/.jsx files exist under ui/src/; 15 are not in the active import graph (orphaned/unused components including AutopilotConsole, ObserveConsole, ServiceHealthPage, PluginRollbackPanel, DataTable, adapters.live, and several type-only modules). All 15 unwalked files were separately verified clean — the only occurrence of a forbidden term in unwalked files is the word "fake" inside a JSDoc comment in AutopilotConsole.tsx (a developer instruction NOT to add fake values), which the scanner's comment-stripping logic would correctly ignore if the file were active.

Playwright proof:
- Command: `npx playwright test --config=playwright.e2e.config.ts`
- Latest full result: 41/41 passed after the fleet-onboarding, job-id upload/start gate, Jobs proof-pipeline UI, Jobs repair/rollback transition wiring, Learning/Agents idle workbench Run boundary, Azure STT/backend reply-voice proof, Settings runtime-readiness ledger, Simple sidebar routing, Source OS runtime-readiness split, Design toolchain proof, Design executor MVP, 3D Generation executor MVP, Observe refresh/reconnect, route-sync, bounded printer telemetry, and Hermes Agent Playwright proof-runner slices.
- Latest focused result: `npx playwright test --config=playwright.e2e.config.ts --grep "Agents|operator action catalog|Roadmap|Source OS|Plugins|Settings Environment|hash route sync"` passed 10/10 after expanding the Hermes Agent OS operator action catalog and decoupling the Agents idle queue from the slower catalog load. Current backend Source OS runtime proof reports `60 APPS`, `36 RUNTIME READY`, `7 AGENT CLI`, `7 SOURCE READY`, `15 SETUP/REPAIR`, `2 BLOCKED`, `53 VERIFIERS`, `24 CLI/SERVICE CANDIDATES`, and `0 UNCLASSIFIED`; the browser proof should show those counts without any unresolved app state.
- Latest fleet focused result: `npx playwright test --config=playwright.e2e.config.ts --grep "onboarded|onboarding"` passed after adding Settings printer onboarding and Printers-tab onboarded row coverage.
- Covered: all 16 primary tabs route without browser errors, Roadmap remains hash-addressable but is not in the primary rail, Dashboard and Simple mode resize without body scroll at operator viewports, Simple sidebar navigation stays in Simple mode while rendering the live tab component, user-facing side rails are persisted/resizable rather than fixed-width on Main nav, Source OS, Settings, Voice, Plugins, and Simple mode, Source OS blocks false-ish module actions, Observe renders live camera cards with V400 endpoint support, S1 remains action-locked, Hermes Agent chat is mirrored across dashboard surfaces with audio attachment proof, Azure reply-voice playback, pending-action approval controls, and Hermes Desktop v0.3.4 compatibility routes expose the real `/health` + `/v1/chat/completions` contract.

Live API truth/security proof:
- `/api/system/snapshot` reports `security_status: Open` unless bearer auth is actually enforced by `HERMES3D_REQUIRE_API_AUTH=1` and a token is configured.
- `/api/settings` rejects camera/Moonraker URLs whose hosts are not configured printer IPs, and rejects credential-bearing or non-http(s) printer URLs.
- `/api/printers/onboard` rejects S1 IP/aliases with HTTP 423, rejects unsafe/non-LAN Moonraker roots, requires live `/server/info` plus `/printer/objects/query` probes before save, persists onboarded printers in SQLite, and appends `printers.onboarded` proof on successful creation. Settings surfaces the exact failed probe, and read-only onboarded printers stay blocked from upload/start in the Printers tab.
- `/api/desktop/compat` reports the Desktop bridge separately from live Hermes Agent runtime availability; current chat compatibility is backed by the trusted local/private runtime at the configured runtime URL, and self-bridge loops remain rejected.
- GUI API launchers now load `G:/private/.env` into backend API processes only; the Vite frontend process is not given private runtime secrets. Current runtime readiness shows Hermes Agent runtime and Idle Learning runner configured from private env, while frontend bundles still receive no secret values. Hermes evidence ledger entry `ev_13689248a20f86bf` records the loader change and 37/37 Playwright verification.
- Hermes Agent runtime URL validation rejects loopback self-bridges on ports `8765` and `8642`; proof probe with `HERMES3D_AGENT_RUNTIME_URL=http://127.0.0.1:8765` returned `None` from both runtime validators instead of creating a recursive proxy loop.
- `/api/desktop/update/status` reports Hermes Desktop source state separately from installer update state. Current local source path `G:/Github/apps/hermes-desktop-main` is source-ready but not a git checkout, so source update is blocked; latest GitHub release installer discovery is live and reports SHA-256 verification support from the release asset digest.
- `/api/system/runtime-readiness` reports the missing-runtime truth ledger without exposing secret values. Latest live probe returned 9 runtimes: 7 ready (`hermes_agent_runtime`, `idle_learning_runner`, `azure_speech`, `proof_key`, `local_llm`, `design_executor`, `printer_fleet`), 2 partial (`source_update_execution`, `generation_provider`), and 0 blocked. Settings Environment renders this same ledger with required env names and proof endpoints.
- `/api/desktop/update/backup` created `20260505T093004Z_0.2.2.source.zip` before installer work. `/api/desktop/update/download` downloaded `hermes-desktop-0.3.4-setup.exe` and verified SHA-256 `6eb6ceeec74018db63b9f26796f29a7d751cafb0c92a04c2b10cca723a01feac` against the GitHub release asset digest. The installer was not executed silently.
- `/api/autopilot/guardrails` reports S1 lock, PRINT_APPROVAL, and truth-gate print-start enforcement as active. A live probe against T1 #1 with `start=true` and no `job_id` returned HTTP 409 instead of starting the printer: `Starting a print requires job_id with approved PRINT_APPROVAL and passing truth-gate records.`
- Printers UI proof: Upload + Start now requires an approved job ID before the button enables, and `uploadGcodeLive` sends `job_id` to `/api/printers/{printer_id}/upload-gcode` so backend PRINT_APPROVAL/truth-gate records can be verified instead of silently omitting the job context.
- Jobs UI proof: job detail now renders a proof-gated pipeline surface from live job detail data: model, slice, bounds, approval, upload, print, observe, complete, repair. It shows the current blocker and required gate, disables repair/rollback controls until backend routes exist, and keeps cancel disabled for backend-noncancellable jobs.
- Jobs transition proof: `/api/jobs/{job_id}` now includes approvals and backend transition state. `/api/jobs/{job_id}/cancel`, `/repair/propose`, `/repair/apply`, `/retry`, and `/rollback` append `job_events` plus `proof_events`; blocked transitions append blocked proof and fail closed. Live proof job `a936044cad8e4b07b378bd53c0ed1187` created REPAIR_APPROVAL `c998960a23e74b7ea1da4aca6390b5b9`, proposal proof `12f27d2e5eae4e0d95caaea28421b5c0`, apply/escalation proof `2421f6f2f80a4abd99b888e5d2eb9d82`, retry proof `cd752dcbed3c4add913c9d2901ed07ab`, and rollback proof `41583d82acb34f4db5df313c9e4b4e10` against checkpoint artifact `39e54d542bcf4e279e7c2eb7f8793349`. The UI buttons now call these routes and surface exact blocked/accepted messages. Apply repair uses the existing read-mostly `RepairAgent`; when no executable repair handler exists it records escalation instead of pretending a file or printer was changed.
- Learning/Agents idle workbench proof: `/api/learning/idle-workbench` exposes a persistent SQLite candidate queue, daily review prompt, live printer/job/approval blockers, proof event IDs, and review approval links. Learning can queue research/app/printer/documentation/workflow candidates, request review, keep/remove decisions, and keeps merge disabled until backend approval plus proof gates pass. Agents renders the same queue state from the live API so the two tabs cannot drift. The backend blocks build/download/install/update/merge style work while printers, open jobs, or pending approvals are active; S1 remains a read-only policy blocker.
- Learning runtime truth proof: `/api/learning/config` separates saved selection from active execution. Latest live readiness shows `idle_learning_runner: ready` and `/api/agents/health` returns `bridge_ready` for model `qwen3.5-9b-glm5.1-distill-v1`. `/api/learning/idle-workbench` reports `runner_status: ready`, `agent_runtime_status: ready`, research `execution_status: ready`, and documentation/workflow/app-update/printer-maintenance blocked by live blockers: FLSUN S1 policy plus queued job `Pilot Calibration Cube`. Hermes evidence ledger entry `ev_fa9d4d6477975717` records the earlier blocked probe; the current runtime probe supersedes that state.
- Learning candidate Run proof: `/api/learning/idle-workbench/candidates/{id}/run` is wired through backend and UI. When runtime and blockers are green for a report-only candidate, it may create a report artifact plus proof event; when a candidate is blocked by policy/job/approval gates, it returns the exact blocked reason and proof. No install/merge/upload/print action runs from this route.
- Latest live idle-workbench probes after API restart returned `status: ready`; the current queue includes the `Learning runtime proof probe` candidate used to prove the Run blocked path, while S1 remains policy-locked and open-job blockers continue to prevent risky idle mutations. Earlier Hermes evidence ledger entry `ev_458a48941ce835db` records compile, TypeScript, no-fake scan, full Playwright 32/32, Simple routing proof, and the first idle-workbench API probe.
- Simple routing proof: Simple mode now uses the same primary tab registry as the main app, with Roadmap intentionally excluded from the visible rail. Browser proof on the in-app tab showed `simple-version-root` stayed mounted while `#autopilot`, `#sources`, and `#agents` mounted their real tab roots inside `simple-live-tab-root`; returning to `#dashboard` restored `simple-dashboard-grid`. The only control that exits Simple remains the explicit Full button. Hermes evidence ledger entry `ev_c33f19ced3792210` records this browser proof plus TypeScript, no-fake scan, and full Playwright 32/32.
- Hermes Agent staged update requires backup, fails closed on skipped or dirty gates, and rollback targets are constrained to recorded backup refs.
- Left rail proof: browser geometry check at 2048x900 showed the Hermes Agents chat mirror, textarea, and send button all remain within the viewport after the flex-column cutoff fix.
- Resizable rail proof: `ResizablePane` gives the Main nav/Hermes Agents rail, Source OS project list, Settings sections rail, Voice agent rail, Plugins API panel, and Simple mode rail user-owned widths in local storage. Browser proof on `#sources`, `#settings`, `#voice`, and `#plugins` found one resize separator per affected surface; keyboard arrows changed `aria-valuenow`, `Home`/`End` shortened/expanded Source OS from 220 to 620, and double-click reset restored the default width.
- Hermes Agent chat proof: the left rail exposes real browser mic controls. When browser media capture is available, the mic records a voice note and uploads it as a local `agent_attachment`; when SpeechRecognition is available, it also fills dictation text. Attachment upload supports Hermes3D model/mesh/CAD/G-code/image/audio/config/proof/source files, records SHA-256 in artifact notes, rejects executable types with HTTP 415, and surfaces `agent_attachment` / `AGENT_CHAT` rows in the Artifacts tab. Live probes uploaded `agent-upload-probe.stl` and `voice-note-live-proof.webm` to `/api/agents/print-safety-agent/attachments`; the `.webm` artifact recorded SHA-256 `143c85e5602da92251114657c4d9c7bbe72fc1f75b44236a48c9f1aef9cad10e`. Chat messages send attachment artifact IDs as context. If `HERMES3D_AGENT_RUNTIME_URL` points to a trusted local/private runtime, `/api/agents/{persona}/chat` streams the real `/v1/chat/completions` response and persists the assistant content; without that runtime it stays truthfully blocked. The browser parser now accepts Hermes message frames and OpenAI-compatible `chat.completion.chunk` SSE frames ending in `data: [DONE]`. The composer also includes quick prompt chips for build-plate review, attached-file review, and safe print planning, plus an Open Agents shortcut.
- Hermes Agent Azure reply voice proof: chat reply playback now uses the selected agent's `/api/voice/agents` Azure assignment and calls backend-only `/api/voice/preview`; the frontend receives only playable audio plus proof metadata. Live probe synthesized Print Safety Agent voice `en-US-AriaNeural` for `Hermes Agent voice proof.`, returned `audio/mpeg` with 15,840 bytes, and appended proof event `e827b99958f34db580b39f1fc87305ae`.
- Hermes Agent Playwright proof runner: `/api/agents/{persona}/playwright-run` exposes fixed `observe`, `smoke`, and `full` scopes only. It stores redacted `agent_playwright_run` artifacts and appends proof events. Live Oliver QA Agent observe proof passed with artifact `ceff7bdfdacf4147b6a9aa91cf81690e` and proof `b06ec95b19bc46c0adb04b4e6542a872`; the first full run truthfully failed on an accessible-name collision (`Voice` nav vs chat voice toggle) with artifact `6845b45d22fd4c048a057fd4ebe6d203` and proof `5b6e472e236d4d97b134d0db4036ea10`; after renaming the chat toggle to `Speak`, the full Hermes Agent run passed 41/41 with artifact `34c127910a6d4020adb1414402936cee` and proof `c3cd5d941f2f42e6b570fbc46ff55c05`.
- Hermes Agent operator catalog proof: `/api/agents/action-catalog` exposes 45 public action contracts with tab, route, risk, proof, approval, rollback, payload requirements, and ready/partial/blocked state, and it does not expose internal handler names. Latest probe returned `39 ready`, `3 partial`, `3 blocked`, `40 callable`, and `0 ready-but-not-callable` rows; `printers.upload_start` now advertises required `printer_id,gcode_path` payload keys instead of being a mystery action. A safe catalog sweep executed 25 read/report actions through `/api/agents/actions/{action_id}` and returned 25/25 accepted/completed proof events, including `agents.health.refresh` proof `2afbb82de4f943c39839c25bf72cda77`, `dashboard.snapshot.refresh` proof `c8b49d3efca445cba8919b0acf18ac0f`, `source.agent_cli_readiness.refresh` proof `eb92dc29c25843a7b1d6b53e8075dd7f`, `observe.cameras.refresh` proof `27877abca51246c49fb3d23e4246d6c2`, and Roadmap truth proof `19f7ecf90f8a4a1ba303bf7fd0ec3142`. Protected missing-payload probes for printer upload/start, camera evidence, job transition, approval decision, and STT all failed closed with 4xx responses. Non-physical artifact actions also passed: local generation proof `783d06b27087489a8914956a5df559dd`, bounded design proof `1ad2e99d3f9541d58b4a38a0e1ff5b16`, T1 #1 camera evidence proof `4a6143a9ef6a47c993530539d31a0dd1`, Azure catalog proof `cb64df49be2f4036b1945d88ce2cd861`, and Azure voice preview proof `e4bbebf265e34035b3e3f47ecb3c4238` / TTS proof `324cbb9c902643a388ef773d83613c61`. Focused Playwright passed 10/10; screenshot proof is `03_implementation/proof/screenshots/agents-operator-catalog-expanded-2026-05-06.png`. Agents can use this catalog to prove what they can run now, what requires payload/approval, what is plan-only, and what remains blocked.
- Azure STT proof: `/api/voice/stt` now routes speech-to-text through Azure fast transcription using backend-only runtime/private-env secrets. Empty audio fails closed with proof event `956ca446a89445ee8818609218a15532`. A live Azure TTS-to-STT round trip generated `audio/mpeg` from `en-US-AvaNeural`, transcribed it back as `Hermes 3D speech-proof.`, and appended proof event `5d9b8a25a1ac454f85ea46e5a59866f4`. The proof payload stores transcript SHA-256 and metadata, not the raw transcript or any key.
- Hermes Agent approval proof: chat rows with a real `action_id` now render Approve/Deny controls that call `/api/agents/{persona}/confirm-action/{action_id}` or `/api/agents/{persona}/deny-action/{action_id}`. No controls render without a backend action id, and stale/not-found action decisions report the backend block reason.
- Roadmap proof: `03_implementation/ROADMAP.md` now tracks the tab-by-tab completion ledger, current UX/UI completion position, full e2e finish queue, and primary work packages requested by the user. `/api/roadmap/tab-completion` exposes that ledger to the Roadmap tab, and the UI renders compact tab cards, next-package cards, and legacy roadmap rows from live backend responses.
- Source app updater proof: `/api/modules/update/readiness` exposes a non-mutating, server-side source update readiness summary for Source OS/Plugins/Settings. The default call is lightweight and only checks configured local path plus git metadata. `deep=true` adds current ref, origin remote, cached upstream-behind count, and dirty-state checks without fetch/pull/build/install. Source OS selected-app Backup, Check Update, Update, and Rollback controls now call backend routes with JSON bodies and reload readiness after each action. Live proof on `blender_mcp_candidates` created backup `20260505T225446Z_blender_mcp_candidates_7636d13bded8` with proof event `fa26b7f476d24de4b74a46f06b182ae5`, completed update check with proof event `c89d2ea4965d4d9182c53e8b71e086b9`, and exercised the gated update route as a no-op/current update with proof event `970908d222734767a55401644878fa4a`.
- Latest readiness probe after API restart: default `/api/modules/update/readiness` returned 60 source modules, 60 update-check-ready, 0 blocked, 0 cached-outdated, 0 dirty. The first cold call completed in 2381 ms; future UI wiring should cache/poll this backend summary rather than running deep checks automatically.
- Source OS/Plugins UI proof: Source OS and Plugins now render a compact Source App Update Readiness summary from `/api/modules/update/readiness`. Source OS also renders a separate Runtime Readiness summary from live `/api/modules`: latest backend proof shows 60 apps, 36 verifier-backed ready rows, 7 source-ready runner gaps, 15 setup/repair rows, and 2 blocked rows. Runtime-state badges are explicit: `ready`, `source`, `setup`, `install`, or `blocked`; the previous unresolved `UNKNOWN` badge is not present in the live `#sources` page after reload. The selected-app Verify button calls `/api/modules/{module_id}/runtime/verify` and appends proof. The Verify All button calls `/api/modules/runtime/verify-all` and runs only safe registered probes. The Setup Queue button calls `/api/modules/runtime/setup-queue`; the live GET route reports the 60 planned records from backend runtime contracts rather than inventing app state. Selected-app Setup Plan calls `/api/modules/{module_id}/runtime/setup-plan` and writes proof without executing any unregistered installer. The visible Update action remains disabled until a recorded backup exists, then calls the gated update route; the Deep Check button only asks the backend for non-mutating local git preflight. Focused Playwright proof covers Source OS/Plugins wiring, no-UNKNOWN list assertion, runtime readiness summary, and false-success action blocking.
- Source OS service URL-key proof: all 10 currently registered local/private service URL keys are present in `G:/private/.env`: FDM Monster, Fluidd, Mainsail, OctoFarm, OctoPrint, Manyfold, Open Filament Database, Kiri:Moto/GridSpace, ComfyUI, and ComfyUI TRELLIS wrapper. Live verification attempted all 10 configured URLs and kept every row `setup_required` because the corresponding service did not answer the expected non-mutating health/version proof. Proof events: FDM Monster `23d8ef38a2074efd80868dfd2d1eaec9`, Fluidd `2d7b854c567a4163803ce7e5ab8e14f2`, Mainsail `02c0e8b6bb72485093d7b61def2b092a`, OctoFarm `a33df8ed7a944f78a23e0631ebd1efb2`, OctoPrint `00ebac59b3234831be708f8bae32befc`, Manyfold `0c7509883a8a41e4adf280da0aff0a87`, Open Filament Database `fdf19109e8084a5aa0cf89bea93f6fb5`, Kiri:Moto/GridSpace `9ae5b627fda044b3ae4822deb067f92b`, ComfyUI `ea093820a76741c981ebc6236d066bba`, and ComfyUI TRELLIS wrapper `d279a094cae74c2e91953d2eddb35ecd`.
- Source OS service setup/start runner proof: `/api/modules/{module_id}/runtime/start-runner` now exposes a proof-writing preflight contract for the 10 local/private service/web rows. It checks the configured URL key, local/private URL guard, source checkout presence, known command family, and port state, then returns `ready_to_start`, `setup_required`, `already_running_verified`, or `unsupported`. It does not launch processes yet; `execute=true` fails closed until the sandbox/process-supervisor gate is implemented, so no app can be falsely marked runtime-ready without the existing health/version verifier passing.
- Observe refresh proof: in-app browser verification on `http://127.0.0.1:5173/#observe` clicked the visible Refresh button. The button changed to `Refreshing`, the route remained `#observe`, the backend returned four configured camera rows, and the UI reported `Camera feeds refreshed: 4/4 configured feeds.` Live cards showed T1 #1, T1 #2, S1, and V400 feeds online; S1 remained action-locked/read-only.
- Route-sync proof: browser hash navigation was verified after hardening the router heartbeat. `#dashboard` mounted the Dashboard without the Observe component, then `#observe` mounted Camera Observer. Refresh stayed on `#observe` and still reported `4/4 configured feeds`, including the V400 live image. This prevents the URL and mounted tab from drifting after in-app browser navigation or hot reload.
- Runtime port fallback proof: launch scripts now choose open local ports instead of hard-failing on `5173`, `8765`, or `8642`. With those three defaults already occupied, a second stack selected UI `5174`, GUI API `8766`, and Desktop compatibility API `8643`; the fallback UI returned HTTP 200 and fallback API `/health` returned `ok`. A clean restart then wrote `03_implementation/var/runtime-ports.json` with UI `5173`, API `8765`, Desktop `8642`, and the in-app browser loaded `http://127.0.0.1:5173/#sources` with Source OS Runtime Readiness visible.
- Printer telemetry proof: `/api/printers` now runs bounded parallel Moonraker probes. When a printer API answers the short live probe it reports `data_source: live`; when the printer API is reachable/configured but too slow for dashboard polling it reports `status: online` with `data_source: degraded` and `status_source: telemetry_timeout_fallback_to_operator_status`. Physical upload/start/test routes still perform hard live Moonraker gates before any printer action.
- Design toolchain proof: `/api/design/toolchain/status` returns source checkout records, local executable audit records, proof-source paths, stage-level readiness, and supported executor templates. Earlier probe `ev_f0ee8b035a8d92b2` recorded the blocked pre-executor state; the current route now reports `overall: ready` when the local parametric desk-organizer executor is importable.
- Design executor proof: `/api/design/intake` now supports the real bounded `desk_organizer` template. Live probe after API restart returned `overall: ready`, `execution_ready: true`, created job `2a5d92b42897401490962dfa0c13e4fe`, wrote STL artifact `desk_organizer_150df888cd.stl` (28,884 bytes), wrote signed proof artifact `desk_organizer_150df888cd.proof.json`, inserted proof event `97dd4ccadf55423cafeb4256f1b7c8c6`, and recorded truth-gate status `pass`. `python -m hermes3d.cli proof verify` verified the proof envelope with HMAC-SHA256; the runtime currently warns that `HERMES3D_PROOF_KEY` is not set, so the default local proof key remains a final hardening item.
- 3D Generation executor proof: `/api/generation/run` now supports the real bounded `calibration_cube` template. Live probe after API restart created job `56954aa084b94a71a3bea4cb38f72f55`, wrote STL artifact `calibration_cube_c8f48130af.stl` (684 bytes), wrote preview artifact `calibration_cube_c8f48130af.preview.svg` (644 bytes), wrote signed proof artifact `calibration_cube_c8f48130af.proof.json` (4,617 bytes), inserted proof event `bff1c6b5608e4384ad2e9c3fe4e5e5b5`, and recorded truth-gate status `pass`. `python -m hermes3d.cli proof verify` verified the proof envelope with HMAC-SHA256; the runtime currently warns that `HERMES3D_PROOF_KEY` is not set, so the default local proof key remains a final hardening item. Unsupported arbitrary prompts fail closed with setup guidance for ComfyUI, TRELLIS.2, or Hunyuan3D instead of inventing output.

Source OS registry proof:
- Script: `python 03_implementation/scripts/audit_source_registry_truth.py`
- Artifact: `03_implementation/proof/SOURCE_REGISTRY_TRUTH_AUDIT.json`
- Result: 60/60 registry entries resolved to installed Git source checkouts, 0 source-available missing checkouts, 0 unavailable/unverified entries. Printrun source proof points to `G:/Github/Hermes3D-OS/source-lab/sources/print-farm/Printrun`; the `G:/Github/apps` Pronsole/Pronterface binaries are recorded separately as local runtime launchers.

Local tooling proof:
- Script: `python 03_implementation/scripts/audit_local_tooling.py`
- Artifact: `03_implementation/proof/LOCAL_TOOLING_AUDIT.json`
- Result: PrusaSlicer, FLSUN Slicer, OrcaSlicer, OpenSCAD, Blender, and CuraEngine CLIs detected from installed program paths and executed only with bounded help/version commands; Bambu Studio, UltiMaker Cura, Pronsole, and Pronterface launchers are detected but not executed by the audit because desktop/serial side effects are not safe to auto-run.
- Runtime verifier proof: live `/api/modules/prusaslicer/runtime/verify` executed `C:/Program Files/Prusa3D/PrusaSlicer/prusa-slicer-console.exe --help`, returned `runtime_ready: true`, and appended proof event `84e425b7fe514d42896ad16083eed561`. Live `/api/modules/flsun_slicer/runtime/verify` executed `C:/FlsunSlicer2.0/FlsunSlicer.exe --help`, returned `runtime_ready: true`, and appended proof event `eb18f6c1b98b4f52b8f955aee4b2503c`. Live `/api/modules/azure_speech_sdk_js/runtime/verify` returned `source_ready` with setup steps and proof event `ae75bb3c53d6418ab7c603c7d7747e90`, proving source checkout availability without pretending a runnable adapter exists.
- Batch runtime proof: live `/api/modules/runtime/verify-all` runs only registered safe probes. The current registered verifier set contains 53 enabled rows: 7 agent-usable CLI probes, 3 desktop launcher metadata probes, 18 read-only source-inventory probes including Marlin, Prusa Firmware, RepRapFirmware, Repetier Firmware, and Smoothieware, 5 backend package/import probes, 5 Python import repair probes, 3 read-only Moonraker/Klipper fleet probes, and 10 local/private HTTP health probes. The added Blender MCP proof is a source import only; it does not start the MCP server or connect to Blender. Firmware source inventory is read-only and exposes no compile, flash, upload, or printer action. Source-only runnable apps stay `source_ready` until a safe runner/verifier is registered.
- Runtime setup queue proof: live `/api/modules/runtime/setup-queue` returns planned work from the same 60-row runtime contract matrix. Current proof shows 36 runtime-ready rows, 7 source-ready runner gaps, 15 setup/repair rows, and 2 blocked rows. Selected-app setup plans remain intentionally non-mutating until app-specific safe runners are registered.
- Source app setup queue visibility proof: the backend exposes read-only GET `/api/modules/runtime/setup-queue` for status surfaces and proof-writing POST `/api/modules/runtime/setup-queue` for planned agent work. After restarting the live API, GET must match the regenerated 60-row runtime contract truth: 36 runtime-ready rows, 7 source-ready runner gaps, 15 setup/repair rows, and 2 blocked rows. User corrected the active target to 60 apps, so the remaining Source OS work is verifier/runner/smoke-gate completion for those 60 rows, not app-count expansion.
- Source app 60-completion proof: `python 03_implementation/scripts/audit_source_app_completion.py` writes `03_implementation/proof/SOURCE_APP_60_COMPLETION_AUDIT.json`, confirming the corrected 60-app target, current runtime-ready count, remaining runner gaps, blocked rows, and status.
- Rust acceleration proof: `03_implementation/rust/hermes3d_accel` adds a read-only Rust helper for CPU-bound SHA-256 proof hashing, bounded G-code metadata parsing, and binary STL triangle/bounds metadata. It is optional: `03_implementation/src/hermes3d/services/rust_accel.py` falls back to Python if `HERMES3D_RUST_ACCEL_BIN` or the local Cargo build output is absent. `parse_gcode_metadata` and proof-envelope file hashing use the helper only when it is present, so no UI route or printer action can falsely depend on Rust. Verification: Cargo unit tests passed 3/3, local Cargo build produced the debug binary, Python wrapper tests passed 4/4 including fallback and built-binary smoke paths, Ruff/py_compile passed, and a direct `hermes3d-accel gcode-meta` proof returned Rust JSON for a bounded G-code fixture.
- Source app verifier registry proof: 53 safe CLI/desktop/source-inventory/package-import/Python-import/fleet/local-http verifiers seed `module_runtime_verifiers` in SQLite and are exposed by `/api/modules/runtime/verifiers`. `python -m py_compile 03_implementation/src/hermes3d/services/module_runtime.py 03_implementation/src/hermes3d/db/init.py 03_implementation/src/hermes3d/api/routes/modules.py` passed, the verifier route reported 53 enabled verifier rows, and the regenerated runtime audits report 60 apps, 36 verifier-backed ready rows, 24 runner gaps/repair blockers, and 2 blocked rows.
- Source app launch-kind proof: `03_implementation/src/hermes3d/db/load_modules.py` now applies conservative loader-side launch-kind overrides for the 19 rows that previously loaded as `unknown`. The latest loader proof reports 60 modules, 0 unknown launch kinds, and still does not promote any of those rows to runtime-ready without a verifier.
- Source OS visible verifier proof: the Runtime Readiness bar on `#sources` now renders the live verifier and classification truth from backend routes: `60 APPS`, `36 RUNTIME READY`, `7 AGENT CLI`, `7 SOURCE READY`, `15 SETUP/REPAIR`, `2 BLOCKED`, `53 VERIFIERS`, `24 CLI/SERVICE CANDIDATES`, and `0 UNCLASSIFIED`. Browser proof showed no visible `UNKNOWN` and no stale `120` claim.
- Source app runner-gap API proof: `/api/modules/runtime/gaps` returns a live 24-row runner-gap breakdown grouped by launch kind and section, with one safe next-verifier recommendation per launch-kind family. Latest proof returned 60 total modules, 36 runtime-ready setup-queue rows, 24 runner/repair gaps, 2 blocked rows, and zero unknown launch-kind rows.
- Source app CLI-first proof: Hermes3D now treats agent CLI exposure as a separate contract. The seven rows with bounded real CLI proof are `hermes_agent`, `prusaslicer`, `orcaslicer`, `flsun_slicer`, `openscad`, `blender`, and `curaengine`. `bambustudio`, `cura`, and `printrun` are detected launcher metadata only and must not be advertised as agent CLI-ready until a safe CLI/API smoke is registered.
- Source app CLI readiness audit proof: `python 03_implementation/scripts/audit_source_cli_agent_readiness.py` writes `03_implementation/proof/SOURCE_APP_CLI_AGENT_READINESS_AUDIT.json` and classifies all 60 rows by agent execution tier: 7 verified agent CLI, 3 launcher metadata only, 5 package/import ready, 3 service/API ready, 18 source-reference ready, 2 CLI install/config preflight rows, 1 npm package metadata preflight row, and 24 runner gaps. The same proof now marks 3 launcher rows as executable-path smoke ready, 5 failed Python import rows as repair-preflight ready, 2 legacy slicer rows as CLI install/config preflight ready, and `azure_speech_sdk_js` as npm package metadata preflight ready.
- Source app read-only runner proof: `/api/modules/{module_id}/runtime/read-only-runner` is registered for the 8 package/import/local API-ready rows: `blender_mcp_candidates`, `model_context_protocol`, `firmware_klipper`, `manifold`, `meshlab`, `trimesh`, `klipper`, and `moonraker`. The route reruns only the existing safe verifier proof and appends evidence; it cannot launch, install, update, write output files, or issue printer commands.
- Source app executable-path runner proof: `/api/modules/{module_id}/runtime/executable-path-runner` is registered for the 3 installed launcher metadata rows: `printrun`, `bambustudio`, and `cura`. The route reads only executable file metadata/hash and appends evidence; it cannot launch apps, install, update, write output files, or issue printer commands.
- Source app Python import repair runner proof: `/api/modules/{module_id}/runtime/python-import-repair-runner` is registered for `cadquery`, `open3d`, `build123d`, `numpy_stl`, and `pymesh`. Route smoke accepted all 5 rows and returned `registered_python_import_repair_preflight`; it reads failed import proof plus source/dependency manifest metadata only and cannot install packages, create environments, start workers, write output files, or issue printer commands.
- Source app CLI install/config runner proof: `/api/modules/{module_id}/runtime/cli-install-config-runner` is registered for `slic3r` and `superslicer`. Route smoke accepted both rows and returned `registered_cli_install_config_preflight`; it reads local source checkout, adapter schema, profile/config, and candidate executable metadata only. Both rows remain `runtime_ready=false` and `agent_executable=false`, and the route cannot install, launch, slice, write output files, update source, or issue printer commands.
- Source app npm package metadata runner proof: `/api/modules/{module_id}/runtime/npm-package-runner` is registered for `azure_speech_sdk_js`. Route smoke returns `registered_npm_package_metadata_preflight`; it reads package.json metadata, script names, lockfile/manifests, and local node/npm executable presence only. The row remains `runtime_ready=false` and `agent_executable=false`, and the route cannot run npm install/scripts, start processes, write output files, update source, or issue printer commands.
- Source app agent CLI API proof: live `/api/modules/runtime/agent-cli-readiness` returns the same all-60 execution-tier matrix for Hermes Agents; latest local proof returned `count: 60`, `verified_agent_cli: 7`, `launcher_metadata_only: 3`, `python_import_repair_available: 5`, `cli_install_config_available: 2`, `npm_package_preflight_available: 1`, and `runner_gaps: 24`.
- Source app CLI-surface audit proof: `python 03_implementation/scripts/audit_source_cli_surface.py` writes `03_implementation/proof/SOURCE_APP_CLI_SURFACE_AUDIT.json`, and live `/api/modules/runtime/cli-surface` exposes it to the UI. Current proof: 7 enabled agent CLIs, 24 local CLI/service signals needing real verifiers, 3 launcher-only rows, and 10 rows with no local CLI signal.
- Printrun adapter proof: `PrintrunAdapter.detect()` resolves `G:/Github/apps/Pronsole.exe`, `G:/Github/apps/Pronterface.exe`, and the extracted `Printrun-printrun-2.2.0` source scripts. Source OS `Launch` for `printrun` now opens the real external Pronterface/Pronsole application and reports the spawned PID; it still sends no serial commands.

Printer dispatch proof:
- Artifact: `03_implementation/proof/T1_DELTA_CALIBRATION_PRINT_PROOF.json`
- Result: 50% delta calibration G-code passed bounds and was uploaded/started through the guarded backend endpoint on T1 #1 (`192.168.0.10`) and T1 #2 (`192.168.0.11`). Post-start Moonraker state reported `printing` for both; the latest observation reported `complete` for both. S1 (`192.168.0.12`) was not probed or written.

Remaining risk:
- The static scan and Playwright suite now prove the active GUI routes, common false-success cases, browser layout at tested operator viewports, and print-start blocking when approval/truth context is absent. They do not prove every possible external source app action or every physical printer state transition; those remain covered by route-level live probes and targeted manual proof runs.
